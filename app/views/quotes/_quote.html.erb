<li id="<%= quote.token %>" class="list-row hover:bg-base-300" data-sortable-update-url="<%= quote_path(quote) %>">
  <div class="flex cursor-grab">
    <%= solid_icon_tag("grip-vertical", class: "my-auto") %>
  </div>
  <% if quote.daily_pick? %>
    <div class="tooltip my-auto" data-tip="Daily Quote">
      <div class="counter font-ubuntu text-4xl font-black tabular-nums underline opacity-30"></div>
    </div>
  <% else %>
    <div class="counter my-auto font-ubuntu text-4xl font-light tabular-nums opacity-30"></div>
  <% end %>
  <div class="list-col-grow my-auto">
    <%= turbo_stream_from quote %>
    <p class="border-l-2 pl-2">
      <span class="font-light"><%= quote.quote %></span><br>
      <span class="font-medium"><%= quote.author %></span>
    </p>
  </div>
  <div class="my-auto hidden flex-col gap-4 sm:flex sm:flex-row">
    <div class="tooltip" data-tip="Edit">
      <%= link_to edit_quote_path(quote), class: "btn btn-soft hover:btn-primary" do %>
        <%= solid_icon_tag("pen") %>
      <% end %>
    </div>

    <div class="tooltip" data-tip="Delete">
      <%= button_to quote, method: :delete, class: "btn btn-soft btn-error hover:btn-error", data: { turbo_confirm: "Confirm deletion of a #{quote.author} quote.", confirm_button: "Delete" } do %>
        <%= solid_icon_tag("trash") %>
      <% end %>
    </div>
  </div>
  <div class="flex items-center sm:hidden">
    <%= render "fab", quote: %>
  </div>
</li>
