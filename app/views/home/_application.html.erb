<%# locals: (application:) %>
<%= link_to application.url, target: :blank do %>
  <%= turbo_stream_from application %>

  <div class="card card-side w-full bg-base-100 hover:bg-base-200 card-xs hover:shadow">
    <figure class="flex-shrink-[0]">
      <div class="size-8 ml-4 mr-2">
        <%= icon_tag application.icon_variant, class: "fill-primary" %>
      </div>
    </figure>
    <div class="card-body overflow-hidden">
      <div class="my-auto">
        <h2 class="card-title text-nowrap overflow-hidden text-ellipsis"><%= application.name %></h2>
        <p class="text-nowrap overflow-hidden text-ellipsis text-base-content/48"><%= application.url %></p>
      </div>
    </div>
  </div>
<% end %>
